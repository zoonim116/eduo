{% extends "layout.twig" %}
{% block content %}
    <p class="h1">Recent repositories:</p>
    {% if(repos) %}
        <div class="card-columns">
            {% for repo in repos %}
                {% include '_partials/homepage/repository_item.twig' %}
            {% endfor  %}
        </div>
    {% else %}
        <p class="text-center">No repositories :(</p>
    {% endif %}


{% endblock %}