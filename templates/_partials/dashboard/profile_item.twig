<div class="card">
    <div class="card-body text-center">
        <p><img src="{{ profile.avatar }}" class="shadow rounded-circle"></p>
        <h4 class="card-title"><a href="{{ path_for('user.profile', {'id': profile.user_id }) }}" >{{ profile.users.firstname }} {{ profile.users.lastname }}</a></h4>
        <p class="card-text"><small class="text-muted">Added {{ profile.created_at | timeDiff }}</small></p>
    </div>
</div>