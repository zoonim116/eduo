<div class="card changes-item">
    <div class="card-body">
        <h5 class="card-title"><i>{{ text.user.firstname }} {{ text.user.lastname }}</i> updated {{ diff.created_at | timeDiff }}</h5>
        <p class="card-text">{{ diff.diff | raw }}</p>
    </div>
</div>