{% extends "layout.twig" %}
{% block content %}
    <p class="h1">All repositories:</p>
    {% if(repos) %}
        <div class="card-columns mt-3">
            {% for repo in repos %}
                {% include '_partials/homepage/repository_item.twig' %}
            {% endfor %}
        </div>
    {% else %}
        <span>there is no repos</span>
    {% endif %}
{% endblock %}